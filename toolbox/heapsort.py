n = int(raw_input())
numbers = map(int, raw_input().strip().split())

def buildHeap(n, arr):
    pass

def heapSort(arr):
    buildHeap(arr):
    repeat (n-1) times
        swap arr[1] and arr[size]
        size -= 1
        siftdown(1)
